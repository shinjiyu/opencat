# Server
PORT=3000
HOST=0.0.0.0

# Admin
ADMIN_SECRET=change-me-to-a-long-random-string

# Upstream LLM (OpenRouter example)
UPSTREAM_BASE_URL=https://openrouter.ai/api/v1
UPSTREAM_API_KEY=sk-or-your-openrouter-key
UPSTREAM_DEFAULT_MODEL=deepseek/deepseek-chat:free

# Public URL (used in token responses)
PUBLIC_BASE_URL=https://proxy.example.com

# Token defaults
DEFAULT_DAILY_LIMIT=100
DEFAULT_MONTHLY_LIMIT=3000
